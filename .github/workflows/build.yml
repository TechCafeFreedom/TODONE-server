# Build テスト、ビルドのフローを自動化
# トリガー: master以外のブランチへのpush、全ブランチに対するPR
name: Build

on:
  push:
    branches-ignore:
      - 'master'
  pull_request:
    branches:
      - '*'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    services:
      # testにmysqlが必要なので
      mysql:
        image: mysql:5.7
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: todone

    steps:

    # set go version
    - name: Set up Go 1.13
      uses: actions/setup-go@v1
      with:
        go-version: 1.12
      id: go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    # Create env files
    - name: Create .env file
      run: |
        cp .env_example .env
        cp sqlboiler_example.toml sqlboiler.toml

    # Run ddl
    - name: Setup DB tables
      run: |
        # ERROR 2013 (HY000)対策
        sudo echo "mysqld: ALL: allow" > /etc/hosts.allow
        sudo mysql.server restart
        # ERROR 2013 (HY000)対策. max_connect_errors でロックされたホストを一回吹っ飛ばす
        mysql --protocol=tcp -u root -ppassword -e"FLUSH HOSTS"
        mysql --protocol=tcp -u root -ppassword todone < ./db/mysql/ddl/ddl.sql

    # install packages
    - name: Get dependencies
      run: |
        go get -v -t -d ./...
        if [ -f Gopkg.toml ]; then
            curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
            dep ensure
        fi
    
    # run test
    - name: Test
      run: go test -v ./...

    # build
    - name: Build
      run: go build -v ./cmd/main.go ./cmd/wire_gen.go 
